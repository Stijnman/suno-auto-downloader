# Backups

The original **v1.0.0** code is preserved in the Git history of this repository (the commit before the v2.0.0 upgrade).

You can restore it with:

```bash
git checkout <previous-commit-sha> -- suno_downloader.py README.md
```

Or view the history:

```bash
git log --oneline
```
